import { Flex } from "@chakra-ui/core";

const Footer = (props) => (
  <Flex
    bg="gray.500"
    as="footer"
    justify="center"
    w="100%"
    py="4rem"
    {...props}
  />
);
export default Footer;
